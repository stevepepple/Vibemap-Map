import App from 'next/app';

import { Provider } from 'react-redux';

import React from 'react';

class Vibemap extends App {

    render() {
        return (
            <Provider store={}>

            </Provider>
        );
    }
    
}

export default Vibemap;